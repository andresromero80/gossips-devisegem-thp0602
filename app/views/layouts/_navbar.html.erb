<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'Projet St. Brieuc', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to 'Accueil', root_path, class: 'nav-link' %>
        <span class="sr-only">(current)</span>
      </li>
      <li class="nav-item">
        <%= link_to 'Base de données', secret_path, class: 'nav-link' %>
      </li>
    </ul>
    <ul class="navbar-nav pull-right">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.name %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to 'Modifier mon profil', edit_user_registration_path, class: 'dropdown-item'  %>
            <%= link_to 'Soumettre une action', new_mission_path, class: 'dropdown-item'  %>
            <%= link_to 'Mes actions', missions_path, class: 'dropdown-item'  %>
            <div class="dropdown-divider"></div>
            <%= link_to 'Déconnexion', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            <!-- <a class="dropdown-item" href="#">Action</a>
            <a class="dropdown-item" href="#">Another action</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Something else here</a> -->
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to 'Se connecter', new_user_session_path, class: 'nav-link' %>
        </li>
      <% end %>  
    </ul>
  </div>
</nav>